# Analytics Module

The runtime analytics logic lives in `backend/app/services/analytics.py` so it can access the database directly from the API layer.

This folder is reserved for:

- offline notebooks or scripts
- exported CSV datasets
- experiment notes for recommendation tuning

